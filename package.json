{
	"name": "birdnest-ndz-reaktor",
  "description": "Birdnest surveillance app",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "cd server && npm install && node index.js",
		"heroku-postbuild": "cd client && npm install && npm run build"
  }
}
